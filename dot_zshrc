#!/usr/bin/env zsh

autoload -Uz compinit
compinit
bindkey \^U backward-kill-line

PROMPT="$fg[cyan]%}$USER@%{$fg[blue]%}%m ${PROMPT}"

. ~/.zsh.d/acorn
. ~/.zsh.d/atuin
. ~/.zsh.d/direnv
. ~/.zsh.d/eksctl
. ~/.zsh.d/git
. ~/.zsh.d/just
. ~/.zsh.d/kubectl
. ~/.zsh.d/oh-my-zsh
. ~/.shell/rc
