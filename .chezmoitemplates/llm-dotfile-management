## Dotfile Management

These instructions are managed by chezmoi.
The file you are reading is a rendered artifact — never edit it directly.
Chezmoi will overwrite any direct changes on the next `apply`.

Environment architecture and source paths are documented in **ENVIRONMENT.md**.
To make changes, edit the source files in the dotfiles repository and run `chezmoi apply`.
