You are violating your bash execution instructions. Stop and read this before continuing.

## What you are doing wrong

You are chaining multiple commands into a single Bash call using shell operators like `&&`, `||`, `;`, or `|`.

Example:

```
cd "/Users/mtm/Documents/Obsidian Vault" && git add "file.md" && git commit -m "message"
```

This triggers an unavoidable approval prompt because shell operators break the tool's permission matching entirely.

> If the command contains protected shell operators the pattern will not match and an explicit approval is required.

No permission setting can suppress this — compound commands are unmatchable by design.

This applies to any command, not just git.

## What you must do instead

Run each command as a separate Bash call:

```
cd "/Users/mtm/Documents/Obsidian Vault"
```

```
git add "file.md"
```

```
git commit -m "message"
```

Or, if the working directory is already correct, skip the `cd` entirely.

Never chain commands with `&&`, `||`, `;`, or `|` in a single Bash call.
