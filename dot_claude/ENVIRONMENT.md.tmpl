<!-- This file is managed by chezmoi. Do not edit directly. Source: {{ .chezmoi.sourceFile }} -->
# Claude CLI Environment Configuration

This environment uses a standard Claude CLI setup managed via **chezmoi**.

## Management & Source of Truth

Configuration is managed as a set of dotfiles. Changes must be made in the source repository and applied using `chezmoi apply`.

- **Source Directory:** `{{ .chezmoi.sourceDir }}/dot_claude/`
- **Active Configuration:** `{{ .chezmoi.homeDir }}/.claude/`

## Extension Mechanisms

We utilize the standard Claude CLI extension tiers.

### Custom Commands
- **Source:** `{{ .chezmoi.sourceDir }}/dot_claude/commands/*.md.tmpl`
- **Active:** `{{ .chezmoi.homeDir }}/.claude/commands/`

### Agent Skills (Plugins)
- **Source:** `{{ .chezmoi.sourceDir }}/dot_claude/plugins/`
- **Active:** `{{ .chezmoi.homeDir }}/.claude/plugins/`

## Behavioral Mandates

Core behavioral rules are composed from shared templates in `.chezmoitemplates/` and rendered into `{{ .chezmoi.homeDir }}/.claude/CLAUDE.md`.
